import { Route, Routes } from "react-router-dom";

import { Approve, Kyc, Login, Order, Register } from "./components";
const AppRouting = () => {
  return (
    <>
      <Routes>{/* <Route path="/" element={<Order />}></Route> */}</Routes>
    </>
  );
};

export default AppRouting;
